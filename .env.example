# Public widget key only. Configure runtime settings in Cloudflare as documented.
# Do not place email credentials in frontend environment variables.
# Indexing is off by default. For approved production, set PUBLIC_SITE_INDEXABLE=true
# in the build process environment (Cloudflare build settings or shell), then rebuild.
# This coordinates HTML, robots.txt and asset headers. It does not deploy the website.
